# Visual spec v1

四个壳必须使用同一组尺寸、颜色和字体约束。允许使用框架原生控件，但不得因框架默认主题不同而改变测试内容的密度。

- 窗口基准：1280x800，最小 960x640。
- 左侧栏：宽 264px；主区内边距 24px；输入区最小高度 112px。
- 消息行最大宽度 760px；列表滚动容器必须支持 10,000 条虚拟化。
- 字体：Windows `Segoe UI`，代码回退 `Cascadia Mono`。
- 亮色/暗色都必须可切换，截图时分别记录主题。

机器可读 token 位于同目录的 `tokens.json`。
